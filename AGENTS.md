# AGENTS.md — eys-image2

鹅鸭杀（Goose Goose Duck）小程序的美术素材生产项目：AI 批量生图（Agnes AI / GPT Image 2）、本地 PNG 压缩、官方素材对照与识别度评估。纯素材仓库，没有可运行的前后端应用。

---

## 1. 目录速查

| 目录 / 文件 | 用途 | 是否入 git |
|---|---|---|
| `official/` | 官方原版参考图（240×240，50 张） | ✅ |
| `official_camps_v3/` | **官方基准素材**（56 角色：鹅24/鸭23/中立9），评估与生成任务的对标来源 | ✅ |
| `official_v4/`、`american_retro_style_v3/`、`chibi_style_v1/` | 压缩后的成套产出 | ✅ |
| `原图归档/` | 未压缩原图 + 风格测试产物 + cat_theme 产出（约 274M+） | ❌ 已 gitignore，本地磁盘保留 |
| `临时脚本/` | 临时/调试/实验脚本专用目录（规则见 §2 第 7、8 条） | ❌ 已 gitignore |
| `美术特征档案/` | 官方美术特征档案、视觉提取提示词、Q版素材识别度评估与重制清单、对照板图片 | ✅ |
| `海报/`、`new/`、`docs/` | 宣传海报、新增素材、用户手册 | ✅ |
| `config/roles.json` | 角色配置 | ✅ |
| `py/` | 全部脚本（见 §4） | ✅ |
| `cat_theme_tasks.json`、`风格测试任务*.json` | 批量生图任务清单（用户自备脚本执行，脚本不在本项目） | ✅ |
| `FILE_TREE.md` | 文件树快照（部分条目已过时，改动目录结构时顺手核对） | ✅ |
| `.workbuddy/` | AI 工作区数据 | ❌ |

命名规范：**新目录/新文件一律语义化中文或英文命名，禁止 v5/v6 式版本号递增**。历史遗留的 v3/v4 目录保持原名，不要重命名。

---

## 2. 硬性规则（红线）

1. **图生图铁律：主体完全不变，仅修改目标元素。** 任何图生图 prompt 必须写明"保持主体/描边/平涂色块 100% 不变，唯一改动是 XXX"，并禁止 3D 化、写实化、加文字水印、裁切。参考 `py/batch_tasks.json` 现有 prompt 写法。
2. **透明背景 PNG 禁止有损压缩**（WebP q82 之类会把透明边缘搞出毛边）。做预览页时直接用相对路径 `<img>` 引用原图 + CSS 控制显示尺寸，**不要起本地 http 服务、不要内嵌有损缩略图**；除非文件体积确实撑爆，才考虑内嵌且需用户确认。
3. **流水线阶段间必须停下等用户确认 checkpoint**（如 Stage1 清晰度增强 → 确认 → Stage2 改背景色），禁止自动连续推进。
4. **批量任务 JSON 先出方案再执行**；生成的大图默认落 `原图归档/`，不进 git。
5. **API Key 安全**：Agnes AI 的 Key 内置于 `py/image_generate_v4.py` / `v5.py` 的 `API_KEYS` 列表。不要把 Key 复制到其他文件、日志、回复或外部仓库。
6. 改动目录结构后更新 `FILE_TREE.md`；做完实质性工作后向 `.workbuddy/memory/` 当日日志追加记录。
7. **临时测试与验证脚本管理规范**：
   - **强制归位**：所有因辅助编码、调试、验证 API、临时复现 Bug 而编写的测试脚本、独立代码片段、临时输出文件，**必须**存放在根目录 `临时脚本/` 下。严禁散落在项目根目录或 `py/` 等工具/源码目录中。
   - **层级归档**：按 `[模块]/[场景描述]_[YYYYMMDD].py` 组织，模块固定为 `生图/`（生图 API 调试、prompt 单张试跑）、`压缩/`（压缩参数实验）、`评估/`（素材统计与识别度量化）、`预览/`（HTML 预览页实验）、`杂项/`（其他一次性验证）；同日多轮加 `_02` 序号。命名语义化，禁止 v5/v6 式版本号后缀。
   - **不可提交**：该目录已加入 `.gitignore`，内容仅供本地演练，绝不纳入 Git 提交。
   - **与正式测试的边界**：本项目目前不存在正式单元/集成测试；未来若引入（需入 git 并进 CI），放 `tests/` 目录，不得与 `临时脚本/` 混用。
8. **文件删除的绝对红线（禁止自主删除）**：
   - **禁止自行删除**：AI **绝对禁止**在没有人工明确指令的情况下，擅自调用终端命令（`rm`、`del`、`unlink`、`git rm` 等）或编辑工具直接删除任何文件或文件夹。
   - **提议与列举机制**：发现冗余文件、废弃脚本或需清理内容时，只能以**表格或清单**形式向人类汇报，内容必须包含：① 文件完整路径；② 该文件的具体作用/功能；③ 建议删除的原因。
   - **执行权归属**：所有删除操作**仅由人类本人确认后手动执行**，AI 不得代劳。

---

## 3. Prompt 编写规范（批量任务 JSON）

- 字段（生图类）：`task_id` / `mode` 或 `type` / `prompt` / `aspect_ratio`(1:1) / `output_num` / `quality`(low) / `resolution`(1K) / `out_path`。Agnes 脚本用 `type`+`size`+`local_image_path`+`output_path`+`model`，photogpt 类用 `mode`+`aspect_ratio`+`resolution`+`out_path`，写前先看同目录现有 JSON 对齐字段。
- 五段式结构：画风段 → 角色外观段 → 标志性道具/表情段 → 质感光效段 → 输出要求段（透明背景、居中、不裁切、无文字边框）。
- 角色识别核心从 `official_camps_v3` 实图提炼（如警长=金色六角星徽、刺客=暗红准星+黑兜帽、呆呆鸟=呆滞错位眼+橙红扁嘴），不得凭空捏造角色特征。权威依据见 `美术特征档案/官方美术特征档案.md`。
- 中文 prompt 内嵌引号必须用全角「“”」，半角引号会破坏 JSON。
- **批量改 prompt 后必须跑关键词断言脚本**（校验禁词 0 残留、必含关键词全命中），历史教训：靠肉眼查会漏 30+ 处。
- 单次 Write 超 30KB 会被截断：长 JSON 分片写入后用 python 合并并 `json.load` 校验。

---

## 4. py/ 脚本与环境

| 脚本 | 用途 |
|---|---|
| `image_generate_v5.py` | Agnes AI 批量生图，**推荐**（全局限流 + 按 Key 熔断，不易 503） |
| `image_generate_v4.py` | 同功能基础并发版（下载重试 10 次） |
| `compress_images.py` | 压缩输出到新目录（安全，不覆盖原图） |
| `compress_inplace.py` | 就地压缩超阈值图片（palette 近无损 / lossless），原子替换 |
| `gen_cat_theme_preview.py` | 生成 cat_theme 图片总览 HTML |
| `check_keys.py` | 检查 API Key 可用性 |
| `run_task.bat` / `run_task.ps1` | 一键跑批 |

**运行环境（二选一）**：
- conda 环境 `eys-ocr`（Pillow + requests 已装）：`D:\program\tools\conda\envs\eys-ocr\python.exe 脚本名`
- WorkBuddy 托管 python（Pillow 已装）：`C:\Users\22322\.workbuddy\binaries\python\envs\default\python.exe 脚本名`

生图命令：`python image_generate_v5.py [-c 其他任务.json]`，跑完自动生成 `image_gallery.html` 网格预览。

**环境坑（本机实测）**：
- Git Bash 的 `ls` / `tail` / `dirname` 会报 shim 错误不可用；PowerShell 输出不回传。列目录/搜文件用内置 Glob/Grep 工具，跑脚本用 python 绝对路径。
- 输出目录要用绝对路径 `D:\program\project\front\eys-image2\...`。

---

## 5. 素材评估基准（写评估报告时遵守）

- 对标基准永远是 `official_camps_v3`（官方 56 角色）。`official/` 是官方小图参考，`official_v4` 是旧版产出。
- 小图标可读性红线（来自 2026-09-11 评估）：主体近黑占比 >55% 在九宫格小图下糊成黑块，生成时近黑应 ≤40% 且每图含一块 ≥8% 面积的高饱和亮色。
- 已知档案错误：`官方美术特征档案.md` 中 D-22「鸭子」描述与实际素材（菜刀图标）不符，引用前先对照实图核实。
- 评估产出落 `美术特征档案/`，对照板用 Pillow 拼图（上=官方、下=待评）。

---

## 6. 协作方式

- 用户非脚本作者，动代码前先出方案文档（markdown，讲清"改什么、为什么、影响什么"），评审通过再执行；纯只读分析可直接做。
- 交付物只覆盖明确要求的范围，不主动扩展（不额外做 bug 分析、不加"顺手优化"）。
- 报告用大白话，关键结论给证据（文件路径、数量、对比数据）。
- 内容合规：海报/图像/文案严禁任何引流、推广元素（小程序平台审核要求）。
